# Knowrary 开发实施计划

> 目标：逐步交付“Markdown 知识文件 → 图谱结构视图 → Inbox → 手工布局持久化 → 历史视图 → Agent 辅助”的本地系统。
> 原则：先打通可验证闭环，再增加自动化；每个阶段都有独立产物和验收标准。

## 1. 总体架构与职责

```text
Markdown vault → 本地服务（解析、索引、布局、写回、监听） → Vue 3 + X6 前端
```

Markdown 负责知识内容和关系；`.knowrary/layout.json` 负责结构视图；`.knowrary/review-log.json` 负责复习记录；`.knowrary/index.json` 是可重建缓存；ChangeSet 是所有 Markdown 写回的唯一入口。

本地服务用 **Python 3.11+ / FastAPI**，与 `tools/knowrary/knowrary.py` 同栈；前端 Vue 3 + X6 的构建产物提交入库，运行期不需要 Node。LLM 由 `.knowrary/llm.local.json` 配置（多 provider，learn / review 双角色），详见设计文档 3.11。

## 2. 阶段总览

| 阶段 | 目标 | 主要产物 |
| --- | --- | --- |
| 0 ✅ | 固化规范和最小数据集 | 规范、样例 vault、relation-types.json |
| 1 ✅ | 打通读取闭环 | 解析器、index.json、schema 校验（`tools/knowrary/core/`，2026-09-10 完成） |
| 2 ✅ | 打通画布闭环 | X6 结构视图、layout.json、拖拽持久化（`server/` + `web/`，2026-09-10 完成） |
| 3 ✅ | 打通安全写回 | ChangeSet、写回、备份、冲突检测（2026-09-11 完成） |
| 4 ✅ | Inbox 与 Digest | 三态流程、草稿放置、批量整理、复习提醒（2026-09-12 完成） |
| 5 ✅ | 视图完善 | LOD 折叠、搜索、过滤、引用卡、便签、图片、手工边拐点（2026-09-12 完成） |
| 6 ✅ | 历史视图 | 时间轴、时间线选择（按分组分类）、回放、有效期过滤（2026-09-12 完成） |
| 7 ⏳ | Agent 能力 | 只落地了 `/api/suggest`（关系建议 / 去重 / 分类）；校验、Curator、合并预览、`proposed_types` 都还没做 |
| 9 ✅ | 学习闭环 | 测验出题、三档反馈、错题本（2026-09-15 完成） |
| 10 ✅ | 学习计划与教练 | plans.json、目标→要点拆解（学习 / 面试 / 领域三种口径）、今日清单、时间划分与可行性判断（2026-09-15 完成）；对话留档与对话里记复习仍未做 |
| 11 | 历史视图增强 | **设计已定稿（设计文档 F4.6），未开始**：层级钻取 + 主干道布局；前置是先攒够带 year 的节点 |
| 12 | 项目化重构 | **设计定稿（`doc/设计文档/项目化重构方案.md`），未开始**：plans→projects、学/考双态、项目画布、学习日历 |
| 补 ✅ | 口径切换与试用修补 | 对话三档口径（教练/面试/聊天，提示词 + 工具白名单，不做多 agent）、全局对话线与对话里建项目、切项目重拉画布、记住上次项目、合并重复的「全局图」入口、修 `nextTick` 未 import（2026-09-16） |
| 五期 ✅ | 学习日历 | `GET /api/calendar`、热力图 + 当天明细、连续天数；**全派生不新增记录**，热力不算模型调用，学习时长按设计不做（2026-09-16 完成） |
| 四期 ✅ | 项目画布与同步到全局 | 多份 layout（`layouts/<项目>.json`）、幽灵占位、同步到全局（落 draft、坐标不搬、重复候选先摆出来）、撤销栈按 layout 隔离（2026-09-16 完成） |
| 三期 ✅ | 视图重排与多会话 | 四个模式（默认对话）、对话全屏 + 右侧图 pane、顶栏项目切换与「↗ 全局图」、聊到哪图上亮哪、点节点带进对话、多会话（留档行标签聚合）（2026-09-16 完成） |
| 二期 ✅ | 学考双态与晨间简报 | 学/考双态（错题本压过自评）、画布双态方块、对话入库后自动落 draft、晨间简报、对话刷新可恢复（2026-09-16 完成） |
| 一期 ✅ | 项目化重构 | `projects.json`（项目 + N 份清单，kind 降到清单一层）、v1 就地升级与迁移命令、按项目过滤今日清单与出题范围、对话留档按项目分目录（2026-09-16 完成，方案见 `doc/设计文档/项目化重构方案.md`） |
| 12 ✅ | 对话式教练 | `chat(messages)` 多轮 + 流式、SSE `/api/chat`、工具白名单（2026-09-20 加原生 function calling，按 provider 能力自动挑，claude-cli 走围栏）、变更卡、对话留档（2026-09-16 完成） |
| 8 | 性能与发布 | 真实数据压测、错误恢复、使用文档 |

## 3. 阶段 0：规范和样例数据

### 工作项

1. 创建 `vault/`、`nodes/`、`fields/`、`assets/` 和 `.knowrary/`。
2. 手动编写 10～30 个节点，覆盖结构、依赖、演化、对照和 stub。
3. 创建 `relation-types.json`，登记关系族、正向类型、互逆和对称类型。
4. 准备无 `year` 的结构节点、有 `year` 的历史节点和有效期节点。
5. 在 git 中提交初始 vault。
6. ✅ LLM 配置文件 `.knowrary/llm.local.json`（模板 `llm.example.json`）与 learn / review 双角色，`knowrary article` 走配置，`knowrary llm list / test` 可查（2026-09-10 完成）。

### 验收

每个节点能被 Obsidian 打开，引用使用稳定 id，样例覆盖未知目标、重复关系和缺失年份。

## 4. 阶段 1：解析器与索引（2026-09-10 完成）

### 工作项

1. ✅ 解析并校验 frontmatter（必填字段、id 合法性、status 枚举、year / 区间、布局字段混入）。
2. ✅ 扫描约定目录（`nodes/`、`fields/`），忽略 `.knowrary/backup`、隐藏目录、README 和临时文件。
3. ✅ 解析 `::` 关系语法，兼容旧箭头语法。
4. ✅ 关系归族、互逆归一（canonical 翻转）、对称边按 id 定向、两侧重复维护合并为一条边、反链用节点 in / out 邻接表表达、stub 识别（`status: stub` 与未创建目标的虚拟占位）。
5. ✅ 输出 `schema_version` / `revision` / `generated_at` / `content_hash` / `nodes` / `edges` / `families` / `stubs` / `stats` / `errors` / `warnings`；原子写入。
6. ✅ 命令 `python3 tools/knowrary/knowrary.py index --vault <path>`（`--stdout` / `--force` / `--strict` / `--out` / `--max-warn`）。计划里原写作 `kg-graph index`，实际统一到既有的 `knowrary` CLI。

### 产物

- `tools/knowrary/core/`：`mdio` / `relations` / `parser` / `index` / `schema` / `diagnostics`，CLI 与阶段 2 的 FastAPI 服务共用同一套解析，`knowrary.py` 变成薄壳，`check` 复用 index 的诊断。
- `tools/knowrary/tests/run.py`：零依赖自测 24 个用例。

### 验收

- ✅ 索引可重建且结果一致：内容哈希相同时 revision 与 generated_at 都不变、不落盘，重复生成逐字节一致。
- ✅ 非法 YAML（`bad_yaml`）、未知目标（`unknown_target`，同时生成虚拟 stub 不丢边）、非法年份（`bad_year` / `year_range_inverted`）都有诊断，另有重复 id、非法关系行、布局字段混入、未登记类型、重复维护、演化缺年份、正文死链等。
- ✅ 修改 / 新增 / 删除 md 后节点和边正确更新，revision +1。
- ✅ 真实 vault（72 节点 / 214 边）0 错误 9 警告，索引契约校验通过。

## 5. 阶段 2：结构视图与布局持久化（2026-09-10 完成）

### 工作项

1. ✅ FastAPI + uvicorn 服务（`server/`，依赖装在项目内 `.venv`）与 Vue 3 + Vite 前端（`web/`，构建产物 `web/dist` 入库、运行期零 Node）；pydantic v2 定义三份契约（index 只读 / layout + LayoutPatch / ChangeSet 留给阶段 3）。
2. ✅ `GET /api/index`（按 md 文件指纹缓存，变化即重建并落盘）、`GET /api/layout`（附 orphans 与 index_revision）、`GET /api/health`。
3. ✅ layout 契约：`schema_version` / `revision` / `updated_at` / `viewport` / `groups` / `nodes` / `refs` / `notes` / `images` / `edges`；首次打开按 `field` 建顶层分组、按 `nodes/` 子目录建二级分组、组内网格排列（生成逻辑在零依赖的 `core/layout.py`，CLI `knowrary layout init` 共用）。
4. ✅ X6 注册纯 SVG 的 `kg-node`（rect + 2 个 text）与 `kg-group`，五个关系族各一种线型；draft 虚线浅填充、stub 灰虚线、孤立记录红色虚线；边标签只在 zoom > 0.8 渲染。
5. ✅ 分组嵌套（parent/children + embedding，拖节点进框即改归属）、节点拖拽、拖空白平移、滚轮缩放、shift 框选、选中后右侧面板显示索引元数据与出入边。
6. ✅ `PATCH /api/layout`：JSON Merge Patch 语义（条目级合并、null 删除、列表整体替换），前端 300ms 节流合并、视口 1s 节流，服务端临时文件 + rename 原子写入。
7. ✅ 启动与每次读取都校验 layout 引用，缺失的节点 / 引用卡 / 边 / 图片 / 分组进 `orphans` 并在画布上以红色虚线显示，不自动删除。

### 产物

- `server/`：`paths` / `contracts` / `index_service` / `layout_store` / `app` + `dev.sh` + `requirements*.txt` + `tests/run.py`（16 个用例）。
- `web/`：`src/api.js`、`src/canvas/{shapes,render,patcher}.js`、`src/App.vue`、`vite.config.js`，构建产物 `web/dist/`；`web/tests/e2e_canvas.py` 用真无头 Chrome 派发真实鼠标事件跑 6 个端到端用例（临时 vault + 独立端口，不动仓库布局）。
- CLI 新增 `knowrary layout init/check`；`core/layout.py` 承载初始布局生成与孤立引用判定。

### 边的可读性（阶段 5 的两项提前做）

72 节点 193 边全画出来是一团麻：结构族 86 条里有 74 条两端同框（嵌套已表达）、26 组平行边完全重叠、全图默认 `router: 'orth'` 绕成迷宫。处理为：

1. 结构族默认不画线（设计文档 3.6），工具条可勾开；
2. 全图默认直线路由，`orth` 只留给阶段 3 的交互式连线；手工调过拐点的边仍按 layout 存的 router 渲染；
3. 同一对节点的多条边按法向错开成分离的弧线；
4. **跨分组边聚合**成「分组 A → 分组 B (n)」一束（粗细随条数），点它展开这对分组之间的明细、再点收起；
5. **悬停 / 选中高亮**：相关边加粗置顶，其余边淡到 0.07。

效果：一屏从 193 条散线降到 60 条组内边 + 16 束跨组边。

### 过程中修掉的三个真 bug（只有真交互才暴露）

1. X6 `embedding.frontOnly` 默认为 true：落点上盖着别的节点时，被拖的节点只解除了旧分组、认不到新分组，归属变成 null。改为 `frontOnly: false` + 自定义 `findParent`（按面积排序命中最内层分组）。
2. 用 `requestAnimationFrame` 放开"初始渲染不写盘"的守卫：后台标签页与无头浏览器里 rAF 不触发，守卫永不放开，用户的拖动被静默丢弃。改为首次真实交互（mousedown / wheel / 点适应窗口）放开，并在入队前与本地镜像比对跳过空写。
3. 程序化重渲染（切关系族、展开聚合束）会重新 `zoomTo` / `translate`，触发视口保存，每次切换都白涨一个 revision。加 `applyingViewport` 标志隔离。

### 验收

- ✅ 打开 <http://127.0.0.1:8765/> 就能看到样例图：2 个顶层分组、8 个二级分组、72 节点、193 边（无头 Chrome 截图核对过族色线型、stub 虚线、工具条统计）。
- ✅ 真实拖拽链路（无头 Chrome 派发真鼠标事件）：拖节点跨分组改归属、拖分组带着子节点一次性落盘、无位移不产生空写、刷新后位置与归属保持且刷新本身不写盘。
- ✅ 拖动后刷新与重启服务位置保持：PATCH → revision +1 → 重启进程再读盘位置不变。
- ✅ layout 修改不改变 Markdown：对全 vault 的 md 做哈希，PATCH 前后一致。
- ✅ 旧 revision 提交被拒绝：返回 409 + `current_revision`，且不写盘；前端自动重新拉取后重放本地改动。
- ✅ 非法补丁（新增条目缺字段、未知字段、引用不存在的分组、自己当父分组、缺 base_revision）一律 422 且不写盘。
- ✅ layout.json 损坏时返回 500 并保留原文件，不覆盖用户数据。

## 6. 阶段 3：节点详情与 Markdown 写回（2026-09-11 完成）

### 工作项

1. ✅ `GET /api/node/:id`：md 原文、元数据、出入边、Obsidian 打开链接。
2. ✅ 右侧详情面板：摘要 / 领域 / 权重、md 原文折叠查看、「在 Obsidian 打开」「改摘要」「放引用卡」。
3. ✅ ChangeSet 契约：`add_edge` / `remove_edge` / `update_edge` / `update_frontmatter`，frontmatter 白名单（布局字段与 id 永不可改）。
4. ✅ 变更预览：`POST /api/changes` 默认 `dry_run=true`，返回每个文件的统一 diff 与变更说明；面板上列出待写回条目，可逐条丢弃。
5. ✅ 写回只改 `## 关系` 区块与被指定的 frontmatter 字段，其余原文（正文、代码块、列表、`## 参考资料`、`## 待办`）逐字保留；没有 frontmatter 变更时连 frontmatter 都不重新序列化。
6. ✅ 写回前快照到 `.knowrary/backup/<时间戳>/<原路径>`。
7. ✅ 写回后索引缓存失效、重新解析并返回新 revision（前端随即刷新）。**未做 WebSocket 推送**：单人使用时发起方自己刷新即可；Obsidian 外部编辑由索引指纹自动发现。
8. ✅ 两道防线：ChangeSet 带 `base_revision`（索引变了就 409）；文件指纹与索引不一致时拒绝覆盖（Obsidian 改过的兜底）。

### 验收

- ✅ 未确认的 ChangeSet 不改文件：预览阶段对全 vault 的 md 做哈希，前后一致。
- ✅ 确认后 Obsidian 可正常打开：写回结果重新解析 0 错误，索引正常更新。
- ✅ 正文、代码块、列表保持原格式：用含代码块 / 列表 / 待办的样例文件断言"关系段以外的字节完全一致"。
- ✅ 外部修改不会被覆盖：改过文件后写回返回 409。

## 7. 阶段 4：Inbox 与 Digest ✅（2026-09-12 完成）

### 工作项

1. ✅ `GET /api/inbox`：列出不在 layout 中的节点，附带按邻居投票算出的建议分组。
2. ✅ Inbox 托盘：「放进去」交给服务端找位置，或直接拖到画布上的某个分组框里（`POST /api/place`，落点由鼠标决定），落下即 `draft`。
3. ✅ `GET /api/digest`：草稿（含超期）、待复习、stub、跨分组桥、重复候选、方向矛盾，每项都能点进画布定位。
4. ✅ 草稿只在已有分组框内找空位，不移动 final。**一处偏离设计**：分组框的初始尺寸是按网格严丝合缝算的，排满时框内一个空位都没有，不许长框等于新节点永远进不来——所以允许把目标分组（连同祖先）**往下长一行**，且只有"长出来才新压上兄弟组"时才放弃。既有的重叠（用户自己把两个框拖重叠了）不算数，否则同样是永久卡死。
5. ✅ 定稿按钮（详情面板）与草稿超期提示（载入横幅 + Digest 标红）。
6. ✅ 复习提醒：`.knowrary/review-log.json`，固定间隔 1/2/4/7/15/30 天算 `next_due`；`GET /api/review/due`、`POST /api/review/:id`；Digest 有"待复习"栏，画布上到期节点右上角点亮金色圆点；命令行 `knowrary review due|done <id>`、`knowrary digest`。

### 验收

- ✅ 新建 Markdown 能进入 Inbox（e2e：写文件 → 重新加载 → Inbox 计数 +1，建议分组跟着邻居走）。
- ✅ 拖入并刷新后位置和状态不变；放置全程不碰 md（md 哈希前后一致）。
- ✅ 判不出分组 / 实在放不下时留在 Inbox，并说明原因，而不是硬塞。
- ✅ Digest 能列出跨分组关系、重复候选与 stub。
- ✅ 只有 `learned` 没有复习记录的节点按 learned+1 天到期；点「复习过了」后 next_due 按间隔表推进，只写 review-log.json。

## 8. 阶段 5：LOD 与结构视图完善 ✅（2026-09-12 完成）

### 工作项

1. ✅ 分离完整图数据（index + layout）和 X6 可见投影（`buildCells`）。
2. ✅ 按缩放级别折叠分组，显示簇摘要。
3. ✅ 只渲染视口内元素（X6 `virtual`），折叠把一屏压到几十个 cell。
4. ✅ 折叠 / 展开与跨分组边聚合：点簇卡片放大进该域、Esc 或「返回全景」退回；跨组边聚合成束、点开看明细。
5. ✅ 关系族过滤、搜索居中、悬停高亮（族过滤 / 悬停高亮 / 跨组边聚合在阶段 2，搜索居中在 2026-09-11）。
6. ✅ 引用卡、便签（2026-09-11）、图片与手工边拐点（2026-09-12）。图片文件放 vault 的 `assets/`，画布只记位置与大小；拖角可拉伸（只对图片开放：知识点卡片的大小是 pageRank 定的，手动改会毁掉"大小=重要性"这条读图规则）。边的拐点写进 `layout.edges[边id].vertices`，双击该边清掉。
7. ✅ 自己的 history 栈管理布局操作（⌘Z / ⇧⌘Z）；Markdown 写回仍走确认流程，不进 undo 栈。

### 验收

超出预算时自动折叠；折叠展开不丢数据；搜索可定位 Inbox、画布和 stub；贴图与拐点只写 layout、不碰 md（e2e 验证）。

## 9. 阶段 6：历史视图 ✅（2026-09-12 完成）

1. ✅ 只画有 `year` 的节点；边按族开关（演化默认开，依赖 / 对照可勾），两端都在图里才画。
2. ✅ X 轴按年份；顶部时间线选择器列出 layout 分组树的任意层级，Y 轴按所选分组的直接子分组分泳道（选"全部"时按 field），泳道内扫描线避让。
3. ✅ 紧凑模式（空白 > 20 年压成固定宽度）、时间滑块与按年回放、节点渐显。**年宽按跨度自适应**（30～130px/年，总宽向 2800px 靠）：真实数据跨 1936–2018，固定年宽会拉出一万像素，适应窗口后整张图缩成一条线。
4. ✅ `被激活` 用金色虚线 + 流动动画。
5. ✅ 多条时间线可多选叠加（块之间留空行）；有效期过滤 `start_year ≤ t < end_year`。

### 踩到的两个坑（都只有真浏览器能暴露）

1. **渐显动画不能动 `transform`**：CSS 的 `transform` 会盖掉 SVG 元素自身的 `transform` 属性，加了位移之后 X6 给每个 cell 算好的坐标全被拍回原点，18 个节点叠成一坨。动画只留 `opacity`。
2. **必须先定视口再建 cell**：X6 的调度器按"当前可视区"决定一个 cell 建好后要不要摆位，先 `mount` 再 `zoomToFit` 的话，落在旧视口之外的 cell 会建出 DOM 却没有 transform。时间轴又宽又扁，首屏之外几乎全中招。改成先 `zoomToRect` 到布局算出来的框、再 mount。
   这两个坑都不改变元素个数，只改变位置——所以 e2e 补了"按年份从左到右、屏幕坐标互不相同"的断言，只数个数是抓不住的。

### 验收

- ✅ 无 `year` 的节点不进入历史图（横幅报有多少个被排除）。
- ✅ 缺年份的演化边显示诊断（真实 vault 上报出 4 条）。
- ✅ 选中某个分组只看到它的节点，泳道换成它的直接子分组。
- ✅ 滑块按年份显示节点和边；勾上有效期后当年已废止的节点消失。
- ✅ 历史视图不修改结构布局：整段操作前后 layout revision 不变（坐标本来就不持久化）。

## 10. 阶段 7：Agent 能力 ⏳（只完成第 1 项的一半）

| # | 工作项 | 状态 |
| --- | --- | --- |
| 1 | learn / review 双角色，统一输出 ChangeSet | ⏳ `/api/suggest` 已落地（review 角色，建议关系 / 去重 / 建议领域，逐条采纳进 ChangeSet）；learn 侧仍只有命令行的 `knowrary article` |
| 2 | 关系校验：结论、置信度、依据、待确认项 | ❌ suggest 只给 `confidence` 和 `reason`，没有"校验一条已有的边对不对" |
| 3 | Curator：给 Inbox 节点推荐分组并说明理由 | ⏳ 分组是 `core.target_group` 按邻居投票算的，**不走 LLM、也没有理由** |
| 4 | 重复检测与合并预览，展示受影响引用 | ⏳ suggest 会报 `duplicates`，Digest 也有"重复候选"，但**没有合并动作**——现在只能手工删一个再改引用 |
| 5 | 跨分组桥 / 孤岛 / stub / 长期 draft 推荐 | ⏳ Digest 已列出这些欠账，但只是"列出来"，没有 Agent 给建议 |
| 6 | 未登记关系只能进 `proposed_types`，不许直接改类型表 | ❌ 三个 prompt 都只说"类型只能从表里选"，**没给模型 `proposed_types` 这个出口** |

新增的能力（原计划里没有，2026-09-15 落地）：`/api/quiz`、`/api/quiz/diagnose`、
`/api/plans/propose`（学习与面试两套口径）、`/api/coach/today`、`/api/rename`、`/api/llm/usage`。

验收：Agent 建议可转成 ChangeSet；未确认不修改 Markdown；每条建议有证据或理由；批量建议可逐条接受、拒绝和撤销。

## 10.5 阶段 9：学习闭环——测验与三档反馈 ✅（2026-09-15 完成）

> 立意：**复习不是"再看一遍"，是"再考一次"。** 原来的复习只有一个「复习过了」按钮，
> 点它等于自己给自己打满分；间隔序号只能前进，答不上来也照样往后拉。这一阶段把
> F8.3 预留的三档反馈补上，并让题目由模型按节点正文和关系现出。

### 工作项

1. ✅ `core/review.py` 升到 v2：显式 `step`（间隔序号）与 `lapses`（忘记次数），
   三档按 F8.3 推进——忘了 → `step` 归 0，模糊 → 不变，记得 → +1。
   `load_log` 就地兼容 v1（日期字符串数组按"每次都记得"折算），**读不写盘**。
2. ✅ 新契约 `.knowrary/quiz-log.json`（`core/quiz.py`）：答题事件流，只追加。
   与 review-log 的分工是**状态 vs 事件**——调度每次都要全量读 review-log，
   不能让题面和作答原文把它撑大。错题本不是第三份数据，是这份事件流按考点聚合的派生结果。
3. ✅ `POST /api/quiz` 出题：prompt `tools/knowrary/prompts/quiz.md`，走 review 角色。
   三种题型对应图的结构——回忆题考正文、**关系题考真实存在的边**、辨析题考对照族邻居。
   题目自带 `points`（考点 node_id），所以"考点关联知识点"是免费的，不需要二次匹配。
4. ✅ `POST /api/quiz/grade` 交卷：答题明细进 quiz-log，每个考点推进一次复习调度。
5. ✅ `POST /api/review/:id` 可选 body 带 `grade`，不带等价于「记得」，旧调用不受影响。
6. ✅ 前端：ActivityBar 新增「学习」面板（今日待复习 + 错题 + 开考），`QuizDialog`
   一题一屏（先想 → 看答案 → 自评三档），Inspector 的单按钮换成「考一下」+ 三档快捷。
   欠账清单不再重复列待复习——图谱的欠账归欠账，我脑子里的欠账归「学习」。
7. ✅ 抽出 `server/llm_call.py`：角色取 provider 与"剥围栏再 json.loads"的容错只写一份，
   suggest 与 quiz 共用（顺手清掉 suggest.py 里一条声明后从未使用的死变量）。

### 补记（同日修订）：加输入框 + 整轮比对诊断

第一版是"心里想一遍 → 看答案 → 自评"，**没有输入框**——试用时立刻暴露问题：不写下来，
自评就是凭印象，而印象最不可靠。改成：

1. **写答案 → 对答案 → 自评三档**（零等待，不调 LLM）。
2. 全部答完后 `POST /api/quiz/diagnose` **整轮一次调用**，逐题给 `missed`（漏掉）/
   `wrong`（记错）/ `suggested_grade` / `comment`。不逐题调：逐题会让每道题卡几秒。
3. **建议档位只许降级**：比我的自评严才提示「下调为 X」，且要点一下才生效；比我松一律不理会。
4. `quiz-log` 增加 `my_answer` / `missed` / `wrong`，诊断本身**只读**，交卷时才一起落盘。

`missed` 与 `wrong` 分开存是这次的关键：漏掉多复习就补上，**记错再考几遍只会把错误记忆焊得更牢**。

实测（故意把 CISC / RISC 的压力归属说反，并漏掉三个 RISC 指令集）：诊断把说反的两处
准确归到 `wrong` 而不是 `missed`，建议档位从我自评的「记得」下调到「忘了」；第二题
类型方向答对但没答指令集，判「模糊」。

### 两个设计决策

1. **批改是自评，不是模型判分。** 自评零延迟零成本，而且"我到底记没记住"只有我自己知道；
   让模型判分只会把一次复习变成一场辩论。
2. **一个节点在同一轮被考到多次，只按最差的一档记一次复习。** 否则一轮答三道题就给这个节点
   记三次，间隔序号凭空跳三级——考得越细，间隔反而被拉得越长。

### 修掉的一个既有 bug（`highlightEdges` 无条件重写边样式）

阶段 9 的 e2e 跑绿之前先撞上两处失败，根因是同一个：`highlightEdges` 退出高亮时
**一律** `removeAttrByPath('line/class')` 并把 `strokeDasharray` 置空。后果两条：

1. 「被激活」边本来自带金色流动虚线（`activationAttrs()` 里的 `class: kg-flow`），
   任何一次高亮复位都把它抹掉，历史视图那条边就不流动了。
2. 更隐蔽的一条：**X6 删属性会让整条边的视图重建，原来的 `<path>` 元素被换掉。**
   这一下如果正好落在 mousedown 与 mouseup 之间，X6 就不再合成 click——
   点边挂不上拐点手柄，而且"第二次点同一条边又好了"，非常像偶发。

改法：每条边把自己的基础样式（`baseClass` / `baseDash` / `baseZ`）记进 `data`，
退出高亮按它还原；并且**值没变就一个字节都不写**（`setLine`），从根上不触发重建。

### 验收

- ✅ core 45 用例、服务层 42 用例、画布 e2e 95 用例全通过。
- ✅ 三档反馈：忘了 → step 归 0 且明天再考；模糊 → step 不变；记得 → step+1 按间隔表推进。
- ✅ v1 review-log 读成 v2 不丢数据，且**读操作不改用户的文件**。
- ✅ LLM 乱答（非 JSON）时返回空题集而不是 500；考点不在索引里的一律丢弃并进 warnings。
- ✅ 真实 vault 端到端：对 ISA / CISC / RISC 出题，三种题型都出来了，关系题问的是
  真实存在的那条 `ISA -实例→ 现代Intel微架构`；交卷后 review-log 与 quiz-log 正确更新，
  **全 vault 的 md 哈希前后一致**。

## 10.6 阶段 10：学习计划与教练 ✅（2026-09-15 完成三块）

方案见**设计文档 F10**，那里记了采纳什么、否决什么、以及为什么，不要重新讨论一遍。

**先记住方向（这一条最初写反过，见 F10.0）**：学习计划是**图谱的施工图，不是图谱的选集**。
目标 → 拆出要掌握的点 → 围绕这些点学 → 节点和边因此被建出来。它和复习 / 错题是**两条线**：
线 A 建设（F10），线 B 保鲜（F8 / F9），只在"一个点被建成后自动进入复习队列"这一处交汇。
**所以它不需要图里先有东西，恰恰是用来把图填出来的**，空图也照样跑得起来。

要点：

1. **教练做两件事，LLM 只参与一件。**
   - **制定计划**（目标 → 知识点清单）：生成任务，**必须走 LLM**；输出是提议，进确认面板，人点了才存。
   - **每日调度** `GET /api/coach/today`：确定性排序，**不需要 LLM**。顺序是
     `逾期错题 > 到期复习 > 当前阶段「未建」的点 > 「只有壳」的点 > Inbox`。
     前两项是线 B、中间两项是线 A；空图时前两项为空，清单从第三项起照样排得出来。
2. **`.knowrary/plans.json`**：`points` 里的 id **允许指向图里还不存在的节点**（这正是计划的用途），
   每个点带 `name` / `why`，形状复用导入方案里的 `stubs`。**待建的点不写进 vault**——
   制定一个计划就凭空多出 20 个空壳会污染 check / Inbox / Digest。
   绑 node_id **不绑簇**（簇是视图状态，重排就失效）；**不存 progress**（掌握度现算，存了必漂移）；
   多计划是 map，同一节点可属多个计划。
3. **掌握度五档**：未建 / 只有壳 / 学过 / 已掌握 / 待复习。前两档是这次补上的——
   一个还没写出来的知识点谈不上"学没学"，它是**还没建**。
4. **出题范围可选**（只考到期 / 只考没学过 / 混合）：`QuizRequest.node_ids` 已经接任意集合，
   纯 UI 改动，后端不动。
5. **对话里自动记复习：只许降级不许升级**（已在 F9.4 的整轮诊断里实现同一条规则）。
6. **对话留档用 JSONL，不建 SQLite、不做 embedding**（门槛与理由见 F10.7）。
7. **蒸馏入库复用 `knowrary context / apply` 与 ChangeSet，不重造**；不做"全自动入库"。

**落地情况**

| 块 | 状态 | 产物 |
| --- | --- | --- |
| plans.json 契约 + 手工编排 | ✅ | `core/plans.py`、`server/plans.py`、`GET/PUT /api/plans`、`panels/PlansPanel.vue` |
| 目标 → 要点（LLM，learn 角色） | ✅ | `prompts/plan.md`、`POST /api/plans/propose`，提议区逐条划掉再「采纳」 |
| 今日清单（不调 LLM） | ✅ | `core/coach.py`、`GET /api/coach/today`、`panels/StudyPanel.vue` 改为清单驱动 |
| 对话里自动记复习（只许降级） | ✅ | `chat.py` 的 `record_review` 工具，提示词里写死"只准记忘了"（`prompts/chat.md` 纪律 3、`chat-interview.md`） |
| 对话留档 JSONL | ✅ | `chat.py` 的 `chat_log_path` / `append_log`，按 `<项目>/YYYY-MM.jsonl` 分文件；会话列表从留档行聚合，不建会话表 |

顺手清掉的一处：`digest` 早先兼管过错题，现在错题只从今日清单出，不留第二个来源。

**实测**（用真实 vault、真实模型）：目标「三个月吃透 Transformer」拆出 4 阶段 29 个点、零 warning；
它认出 `Transformer` 已在图里没另起新名，还把 FlashAttention 挂到已有的「存储器层次结构」上——
正好是诊断报告里"跨领域边为 0"缺的那种桥。`coach/today` 在实盘上排出 7 条逾期复习。

**验证范围**：server 的 `coach` 6 条、`plans` 9 条、受影响的 `digest` / `quiz` 全过。
未跑 core 全量与画布 e2e——这一轮没碰渲染与解析。

## 10.7 阶段 11：历史视图增强（设计已定稿，未开始）

方案见**设计文档 F4.6**。一句话：**链 ≠ 分类，两种意图得用两种布局。**

- 看"AI 全景"是分类——现有的「泳道 + 按分组取色」已经够用，**不用改代码**。
- 看"NLP 这一条线"或"注意力这一支"是链——现在会挤进单个泳道靠扫描线避让，
  变成散点加连线，**读不出谁接谁**。要加**主干道布局**：把最长的那条 `演化为` 链拉成
  Y 固定的水平主轴，旁支挂上下；**X 仍锁死年份**，3.8 的前提不破。
- 顺带补**层级钻取**（点泳道标题钻进去、Esc 回上层），复用结构视图 `enterGroup`/`exitGroup` 那套手势。
  和时间线选择器分工：选择器管"同时看哪几条"，钻取管"看多深"。

**前置是数据不是代码。** 历史视图要求边的两端都有 `year`；2026-09-15 时 73 节点里只有 19 个有
`year`、全图 3 条边。先用阶段 10 的学习计划 + 今日清单把 AI 史那些点建出来，再做布局才看得出效果。

## 10.8 阶段 12：项目化重构（设计定稿，未开始）

**方案全文见 `doc/设计文档/项目化重构方案.md`**，那里有完整的数据模型、改动面（精确到行号）、
分期与验证步骤。这里只留最容易被忘掉的四条判断：

1. **项目是「视角」，不是「容器」。** 项目只引用一组 node_id，不拥有节点。
   因为知识图谱和复习调度都必须全局唯一——同一个大脑不可能"在 NLP 项目里记得、
   在 Transformer 项目里忘了"。
2. **不做显式父子关系。** 交集 / 子集 / 后来的父级，靠选集重叠全部自动成立。
   要表达层次就在图上连 `包含::` 边——层次是知识层的事，项目层再建一套必然漂移。
3. **考试和错题本不建实体**，按项目范围过滤 `quiz-log` 现算。给每个项目建自己的错题本，
   同一节点在两个项目里答错就会有两份记录。
4. **知识层永远不进数据库**，行为层（review / quiz / usage / chat）才是候选，
   触发条件写在方案 §7。

分四期：① projects.json + 迁移 + 按项目过滤 → ② 学/考双态 + 晨间简报 →
③ 项目画布与同步到全局 → ④ 学习日历。对话式教练并行，按方案 §8 的契约走。

## 10.9 阶段 13：导入流水线（2026-09-18 起，六步走到第二步）

2026-09-18 拍板的六步：① 项目画布去父框 → ② 导入产物三分 → ③ 导入入口进 Web（单篇、带项目参数、
三级匹配、认领幽灵）→ ④ 严格串行的批量队列 → ⑤ Inbox 增强（零边标记、一键建域框、孤立提议）→
⑥ 概括节点菜单（分组框与"概括节点 + 包含边"并存）。

### ① 项目画布去父框 ✅

- `core.build_project_layout` 不再生成 `g-list-N`：已建的点按阶段成列（先学的在左），
  幽灵集中停在已建区右下方当"待学区"。
- 分组菜单「解散这个框」：框删掉，点 / 子簇 / 便签留在原位改归外一层。存量 transformer、ai 两张画布已解散。
- 同步条「N 个点还没上画布，放到右下角」：清单后来加的点、被手动去掉的点，点一下才补，不自动长回来。
- 顺手修掉：项目画布上新建 / 建幽灵曾拿项目 revision 打只写全局图的 `/api/place`，
  失败被吞后再把全局 layout 读回来顶掉项目画布。现在项目画布落位走本地补丁（`App.vue::placeLocal`）。

### ② 导入产物三分 ✅

一篇笔记的方案 JSON（`prompts/article.md`）翻成三种产物（`core/importing.py::translate`）：

| 产物 | 落到哪 | 怎么写 |
| --- | --- | --- |
| 新建节点 | `create_node` + `add_edge` | 与画布右键建点同一条 `/api/changes` 通道 |
| 补充老节点（`enrich`，旧名 `merge_into` 仍认） | `append_body` | **只追加**，开头一行 `> 补充自《来源》（日期）：why`，不碰老正文 |
| 待审边 | `.knowrary/pending.json`（第五份契约） | 连到**已有节点**且 `confidence < 0.8` 的边不进 md；新↔新直接写 |

- CLI `knowrary apply` 和 `POST /api/import` 共用这一份翻译；dry-run 给每个文件的 diff（新文件全文）。
- 落盘：`core.commit` 自动备份 → 记 pending → 方案与变更集存档 `.knowrary/imports/` → 索引缓存作废。
- 契约：`server/contracts.py::ImportRequest / ImportResult / PendingEdge`；`core/pending.py` 只有
  load / add（按 `源->目标#类型` 去重）/ remove 三个动作，审核入口留给后续阶段。
- 边界：正文里带 `## 关系` 段被截掉并警告；已存在的 id 不覆盖、提醒改 enrich；正文链到不存在的 id 自动补 stub；
  旧方案没 `confidence` 字段按 1.0 处理，不会因此全进待审。

### ③ 导入入口进 Web ✅

- **入口**：活动栏「导入」面板（项目 / 全局两个作用域都有），不放进对话——一篇长文会把对话历史吃光，
  拆解也该是一次专门的 learn 角色调用。文章三种来路：粘贴 / 仓库里的 md·txt（`GET /api/import/sources`，
  节点目录与代码目录之外的全部；`GET /api/import/source?path=` 读，越界与非素材目录一律 422）/ 本地文件（浏览器里读，不上传）。
- **`POST /api/import/propose`**：一次 LLM 出方案，顺手 dry-run 把 diff 带回来。三级匹配在服务端算完：
  · **认领幽灵**——提示词多一段"当前项目里还没建的点"（`core.article` 拼装，CLI 同一份），模型写 `claims` 的节点 id 直接换成清单 id，
    连带改所有指向它的关系和正文链接（`core.rename_in_plan`）；
  · **近似撞名**——没认领但名字和某个清单点很像（子串或相似度 ≥ 0.6），卡上给「改用清单里的 id」一键；
  · **孤立**——按方案里的连通块算：整块没有一条边碰到已有节点、也没认领的才报，模型顺手给 `suggest_home`（只是建议，不自动建）。
- **审核卡**照对话变更卡的样子：按节点一行（id、摘要可改、认领标记、正文可展开改）、补充老节点清单、
  **待审边可勾选直写**（`promote`，按 `源->目标#类型` 的 key 抬到置信度 1）、提醒、每个文件的 diff；「重算 diff」和「写入」都打 `/api/import`，
  `renames` / `promote` 由服务端改方案再翻译，前端不自己拼改写。**先提升再改名**：key 是按卡上显示的 id 拼的。
- **落位**：项目下认领的点原地从幽灵转草稿，其余新点停右下角（`App.vue::parkNodes`，和「放到右下角」同一处），全部补进清单；
  全局走 `/api/place` 按邻居投票，放不下的进 Inbox。补充过的老节点只刷索引，位置不动。

### ④ 严格串行的批量队列 ✅

- 全在导入面板里（`ImportPanel.vue`），服务端没有新东西：队列是前端的，一次只打一个 `propose`。
- 入队三条路：仓库文件列表勾多篇「加入队列」/ 本地文件一次选多个 / 粘贴一篇「加入队列」。同路径或同名未完成的不重复入队。
- 推进纪律：`waiting → proposing → review → done | skipped | failed`。**上一篇点了「写入」或「跳过」之后才起下一篇**——
  后一篇拆解时服务端索引已含前一篇刚建的节点，能连上、能看出重复。拆解失败停在那一篇等人「重试 / 跳过」，不自动往下烧钱。
- 写入后推进放在 `finally` 之后：`runNext` 的守卫看 `working`，写入还没收尾就起下一篇会被挡回去（改的时候踩过）。
- 队列只活在内存里，刷新即清；已写入的文件不受影响。审核卡头上标「第 k/n 篇 · 名字」，队列区在来源区之外常显。

### ⑤ Inbox 增强 ✅

- **零边标记**：degree 为 0 的条目把「N 关系」换成警示 chip「0 关系 · 让 AI 建议」，点了直接选中它、开检查器、要一轮 `/api/suggest`
  （Inbox 里的点不在画布上，`gotoNode` 跳不过去，所以单独走 `App.vue::suggestForInbox`）。放上去就是孤点，投票也算不出分组——先补边。
- **一键建域框**：`InboxItem.field_group_missing` = 有 field、判不出分组、且画布上没有同名顶层框（判不出分组的根因）。
  按钮「建「X」域框并放入」→ `POST /api/place` 带 `create_field_group: true`：服务端 `core.plan_field_group` 在整张图最下面开一个同名顶层框
  （加法，不动已有内容；两行四列的量，放满了 place_or_grow 自己往下长；泳道留给 regroup 的 create_lane），再把节点放进去；
  同一批第二个同领域的复用那个框。`PlaceResult.created_groups` 单列，不混进 grown_groups。
- **孤立提议**：不另调模型——导入落盘时把方案里的 `suggest_home` 记进 `pending.json` 的 `homes`（只记这次真建出来、又被模型点名孤立的 id），
  Inbox 条目上显示「建议归到领域 / 项目「X」——why（来自《文章》）」；放上画布后条目消失，建议自然不显示。同一批节点再导会覆盖旧建议。

### ⑥ 概括节点菜单 ✅

用户定的：分组框（排版）和"概括节点 + 包含边"（知识）**都要**，正文由模型先起草、人再调（选了 B）。

- **两个入口**：分组菜单「把框里的 N 个点概括成一个节点…」（≥2 个点且框还没绑总览）；框选（Shift 拖框 / 点选）≥2 个点后右键其中一个
  「把选中的 N 个点概括为一个节点…」。菜单上下文多了 `selectedIds`（`graph.getSelectedCells` 过滤 kg-node）。
- **`POST /api/summarize`**（`server/summarize.py`，提示词 `prompts/summarize.md`）：learn 角色一次调用，输入子节点的摘要、正文节选（500 字）
  和已有关系，输出 name / desc / body / layer / year；回答里混进 `## 关系` 截掉；最多 40 个点；不在索引里的 id 被过滤，剩不到两个 422。
- **新建对话框**预填草稿：名字 / 摘要 / 正文都挂「AI 建议」标记，改过就消失（和 layer / year 同一套做法）；对话框显示「包含：…」。
  **起草失败也照样开对话框**，只是正文留空——概括是人的判断，模型垫不出稿不该挡着人建节点。
- **写入一次到位**：同一份 ChangeSet 里 `create_node`（带 body）+ 每个子节点一条 `add_edge 包含`（写回通道支持给刚建的节点挂边）。
  框菜单进来的建完绑成该框的总览（`bindDoc`）；框选进来的可勾「顺手建一个框把它们圈起来」：按子节点包围盒建框、把它们归进去、新点落在框底一行、绑总览。

至此 2026-09-18 拍板的六步全部完成。

## 11. 阶段 8：性能、恢复与发布

1. 生成 1000、3000、6000、10000 节点测试数据。
2. 测量索引生成、首屏、拖动、缩放、折叠和内存。
3. 使用真实节点、分组、便签、图片和边样式重复压测。
4. 验证服务重启、索引重建、layout 损坏恢复和备份恢复。
5. 验证 Obsidian 外部编辑、多标签和写锁冲突。
6. 增加日志、错误页、诊断导出和最小使用说明。
7. 将 schema 版本、迁移策略和兼容性写入发布文档。

发布标准：任何写回失败都不丢原文件；6000 节点以内通过 LOD 保持可操作；超规模时明确降级；首版不承诺外部图稿迁移和多人协作。

## 12. 推荐开发顺序

```text
规范与样例 → 解析器与 index → layout 读写 → X6 结构视图
→ ChangeSet 安全写回 → Inbox / Digest → LOD 与搜索
→ 历史视图 → Agent → 性能与发布
```

每完成一个阶段都保留可运行版本，用真实 vault 做一次端到端验证。后续阶段只能依赖稳定的数据契约，不直接读取前端内部状态。
