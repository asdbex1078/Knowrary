---
name: 栈内存
field: 计算机系统
tags:
  - 内存模型
  - 程序内存模型
desc: 指令流动态分配私有数据，怎么分？栈底寄存器、栈顶寄存器，由于内存之间是连续的，且又有高低之分 → 我们的指令流中有定义了加减操作 → 按照栈内存的增长方向 →…
source: 知识图谱zhis.jpg
layer: 系统软件
timeless: true
---
# 栈内存

指令流动态分配私有数据，怎么分？栈底寄存器、栈顶寄存器，由于内存之间是连续的，且又有高低之分 → 我们的指令流中有定义了加减操作 → 按照栈内存的增长方向 → 加减操作即可。

结构：栈顶 → 代码段A(栈帧) → 代码段B 栈底地址 → 代码段B(栈帧) → 栈底。

## 关系
- 包含:: [[栈帧]] — call 之后把调用者的栈帧地址记在被调用者栈帧底部
- 使用:: [[专用寄存器]] — 栈底寄存器 / 栈顶寄存器
