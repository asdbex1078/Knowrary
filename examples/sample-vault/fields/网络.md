---
name: 网络
field: 网络
type: 领域总览
desc: 一个最小示例领域：HTTP 三个版本的演进，以及它们脚下的传输层
layer: 系统软件
---
# 网络

这是 Knowrary 自带的**示例领域**，用来演示一张图该长什么样——你可以照着改，也可以整个删掉
（`nodes/网络/`、`nodes/_stubs/`、本文件一起删，图就空了）。

```
[[http]] 的三代
   [[http-1-1]] ──演化为──> [[http-2]] ──演化为──> [[http-3]]
        │依赖             │依赖              │依赖
      [[tcp]] ←──对比──> [[udp]] <──基于── [[quic]]
```

一条主线：**队头阻塞**（[[head-of-line-blocking]]）这个问题被踢了两次皮球——
HTTP/2 在应用层解决了它，却卡在 TCP 那一层；HTTP/3 干脆换掉传输层。

## 图中留的坑（待学）
- [[head-of-line-blocking]] 还是个空壳（stub），正等着谁来写
