# 2026-09-14

## Knowrary 笔记体系诊断

对 Knowrary 个人知识库做了一次全面体检（实读仓库 + 量化 `.knowrary/index.json`），产出诊断报告
`doc/诊断报告-2026-09-14.md`。

**实测数据（index revision 7）**：72 节点 / 193 边 / 13 warning / 0 error；
`by_field` = 计算机体系结构 62 + AI-Agent 10；平均度 5.36；有 year 的仅 18/72。

**核心结论**：架构方向正确（md 为唯一真相源、layout.json 外置视图状态、index.json 可重建、
5 族类型化关系、规范校验 + 迁移报告），问题集中在连通性与沉淀比例。

**发现的问题（按严重度）**：
1. P0 图谱断成 3 块：61（计算机体系结构）+ 10（AI-Agent）+ 1（fields 总览游离），
   **跨领域边 = 0**；AI-Agent 无领域总览。
2. P1 `fields/计算机体系结构.md` 度 = 0，因为所有 `[[ ]]` 写在代码块里（解析器与 Obsidian 都不认）。
3. P1 13 处关系环，集中在编译工具链（ISA↔LLVM、GCC↔ISA、GraalVM↔JIT、IR↔LLVM、C语言 环）；
   根因是"实现/实例"与"依赖"方向混用。
4. P2 13 张卡关系区为空（总线、运算器、控制器、RAM、字节码、哈佛架构…）。
5. P2 13 处链接只写在正文、未进 `## 关系` 区 → Obsidian 图谱与结构视图不一致
   （AI-Agent 的 4 张卡几乎全靠正文链接互连，这也是它成岛的原因之一）。
6. P2 元数据补全率低：type 缺 63/72，year 仅 18/72。
7. P3 规范与实践冲突：规范要求英文/拼音文件名，实际全中文文件名。
8. P3 子领域语义（01-07 目录）只活在路径里，未进 frontmatter（field 全部是"计算机体系结构"）。
9. P3 漏斗中间层断裂：day-info 自动收集在跑，但 harness/ 仅 2 文件、llm/ 空目录，
   71 张卡中 62 张的 source 是 `知识图谱zhis.jpg`（旧图一次性迁移）→ 收集池在膨胀、知识库未增长。
10. P4 第一版设计文档未归档；规范第 5 节类型枚举滞后于 relation-types.json。

**给出的行动清单**：今天可做 4 项（修 MOC、补 13 张空卡出边、正文链接并入关系区、重建索引）、
本周 3 项（消环、建 AI-Agent 总览 + 造桥、补 type）、长期 4 项（加 subfield、定文献笔记规则、
更新规范、归档旧设计文档）。
