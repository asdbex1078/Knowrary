# AI技术发展史 这个项目单独的规矩

- 这个项目我要的是**时间线**：每建一个点，先把 `year` 和 `layer` 定下来，
  再决定正文写多深。年份定不了的点，先别建。
- 每个点都要回答一句"它解决了前一代的什么问题"——这句话就是 `演化为` 那条边的理由。
- **跨层的点燃关系优先于同层的细节**：深度学习 → GPU、LISP → 早期 AI 这类 `被激活` 边，
  比多讲一个变体有价值。
- 我不需要公式推导，需要"为什么那一年突然就成了"。
