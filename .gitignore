# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/

# Logs & env
*.log
.env

# ---------------------------------------------------------------------------
# Knowrary：这个仓库只有程序，知识库是另一个目录
#
# 2026-09-22 拆分：nodes/ fields/ assets/ harness/ .knowrary/ .obsidian/ 和
# relation-types.json 全部迁到 https://github.com/asdbex1078/HunDun（历史一并抽走）。
# 程序读哪个库，由 ~/.knowrary/config.json 决定（设置 → 知识库），出厂种子在 seed/。
#
# 下面这几条是**护栏**：万一在仓库目录里顺手初始化了一个库，别让它悄悄进代码仓库。
# ---------------------------------------------------------------------------
/nodes/
/fields/
/assets/
/harness/
/.obsidian/
/.knowrary/
/relation-types.json

# 示例库是个**真库**，人可以切过去随便改，所以它的内容要入库——
# 复习记录、错题本、那篇待导入的素材都是示例的一部分（它自己不带 .gitignore，
# 建新库时由 seed/ 那份负责挡流水）。只有这份派生缓存不必进来。
/examples/sample-vault/.knowrary/index.json

# LLM 配置含密钥，绝不入库（模板在 seed/.knowrary/llm.example.json）
*.local.json

# Day-Info：推送凭证（PAT / Deploy Key），绝不入库
.secrets/

# ---------------------------------------------------------------------------
# Knowrary：个人流水数据不入库
#
# 仓库是公开的，而 .knowrary/ 里混着两类东西：**成果**（手工摆位、学习计划——丢了要重做，
# 值得进版本库当备份）和**流水**（我问过什么、花了多少钱、哪道题答错了——进了公开历史就收不回来）。
# 下面这几条只挡流水，layout.json / layouts/ / plans.json / projects.json / coach* 照旧入库。
#
# **注意：.gitignore 管不住已经被跟踪的文件。** 这个坑踩过一次——`.knowrary/chat/` 这条
# 2026-09-16 就写了，但那三个文件早已被 git 跟踪，规则在这儿摆着完全没生效，
# 对话内容照样一路进了公开历史。以后往这里加规则，**必须同时**跑一次：
#     git rm -r --cached <路径>      # 只从索引移除，磁盘上的文件不动
# 然后 `git ls-files .knowrary/` 复核一遍，确认它真的不在跟踪列表里了。
# ---------------------------------------------------------------------------

# 对话留档是过程不是知识（F10.7），按月分文件，找旧对话用 grep
.knowrary/chat/

# 调用量与花费
.knowrary/llm-usage.json

# 答题与复习流水：答错了什么、什么时候复习的
.knowrary/quiz-log.json
.knowrary/review-log.json
.knowrary/question-pool.json

# Knowrary：对话续接缓存与 claude -p 会话表（复盘 §11.2）。**都是可丢的缓存，不是真值**——
# 对话的真值是 .knowrary/chat/ 的留档，删掉这两份只是下一轮多花一次全量重发的钱。
.knowrary/cards.jsonl
.knowrary/turns.json
.knowrary/cli-sessions.json
